node_modules
.next
out
.env
.env.local
*.tsbuildinfo
next-env.d.ts
.env.supabase
.env.*.local

# Material de referência de terceiros (Formly / wireframe) — não faz parte do código; contém SVG de ~38 MB
design_system/
# Configuração local de ferramentas
.claude/

# Gerados por `npm run conteudo` (fotos otimizadas + dados do site)
/public/media/
/src/generated/
/.cache/
data/
